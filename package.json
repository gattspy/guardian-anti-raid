{
  "name": "guardian-anti-raid",
  "version": "1.0.0",
  "description": "Multi-server Discord anti-raid and moderation bot",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node index.js",
    "deploy": "node deploy-commands.js",
    "check": "node --check index.js && node --check antiRaid.js && node --check database.js && node --check config.js && node --check deploy-commands.js",
    "build": "npm run check && npm run deploy"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "discord.js": "^14.21.0",
    "dotenv": "^17.0.0",
    "express": "^5.1.0",
    "pg": "^8.16.3"
  }
}
